# R-13 Web 服务（EX-01）占位验证证据

- 入口: `我的 -> Web 服务（EX-01）`
- 步骤:
  1. 仅验证 `Web 服务（EX-01）` 入口展示状态与占位提示。
  2. 连续点击入口并返回重进后再次点击，确认提示语义与状态稳定，不进入实现链路。
  3. 明确记录本项为 `占位` 验证；不包含任何 webService 启停、配置或网络流程验收步骤。
- 结果: `blocked（占位验证）`
- 异常分支: 重复点击与返回重进后，若出现可进入 webService 功能实现链路，立即记录为阻塞偏差并停止扩展验收。
- 处理动作: `保持 EX-01 为 blocked 占位；仅记录入口占位提示与重复点击稳定性证据，不进入 webService 实现验收。`
- 验证命令: `rg -n "Web服务不在本轮迁移范围|_showWebServiceNotImplemented|my_menu_webService|onTap: _showWebServiceNotImplemented" lib/features/settings/views/settings_view.dart`
- 命中摘要: `settings_view.dart:142,145,230,237`
- 关联锚点: `#my-final-regression-r-13`
